# Storage mode: 'server' (default) or 'local'
# - server: Uses Express backend + SQLite database (requires Docker/backend)
# - local: Uses browser IndexedDB storage (no backend required)
VITE_STORAGE_MODE=server

# API proxy target (only used in server mode during development)
VITE_API_PROXY=http://localhost:3000
